A **modified** version of **WongKinYiu's** base detect file

When executed as a standalone:

- Shows the directory at which the labels detected are being stored.

- Now provides dynamic unit-selection (i.e, CPU or GPU) based on user selection. (Default = device-defined based on power).

